const app = document.querySelector('#main');
const banner = document.querySelector('#demo-banner');
const announcement = document.querySelector('#announcement');
const ORIGIN = 'https://sideload-readiness.sociobot.in';
const PRODUCT = 'sideload-readiness';
const DEMO_REPORT = { score: 83, device: { id: 'device-6f31a0b2', android: '15 (sample)', usb: 'mtp,adb' }, findings: [
  ['Authorized USB debugging', 'ready', 'One authorized sample device is visible to adb.', 'Keep the authorization prompt accepted while checking.'],
  ['Developer options', 'ready', 'Developer options are enabled.', 'Leave this unchanged for approved maintenance.'],
  ['USB data mode', 'ready', 'The sample device exposes adb over USB.', 'Use a data-capable cable if this changes.'],
  ['Free data storage', 'ready', '2.8 GiB free on /data. The 1 GiB safety floor is met.', 'Keep the floor before copying an update.'],
  ['Package signer visibility', 'ready', 'The installed sample package exposes signing details.', 'Compare the signer with the approved update before installing.'],
  ['Recovery update visibility', 'needs-review', 'A/B update support is visible, but Android cannot safely prove recovery sideload status while running.', 'Read your device’s approved recovery instructions before using recovery.']
] };

function reportMarkup(report = DEMO_REPORT) {
  const items = report.findings.map(([name, state, what, next]) => `<li class="finding"><strong>${name}</strong><span class="state ${state}">${state.replace('-', ' ')}</span><p>${what}<br><b>Next:</b> ${next}</p></li>`).join('');
  return `<section class="report-shell" aria-labelledby="report-title"><div class="report-top"><strong id="report-title">READINESS REPORT / SAMPLE</strong><span class="score">${report.score}% · Fix one item before recovery</span></div><ul class="findings">${items}</ul><div class="checklist"><h3>Recovery checklist</h3><ol><li>Keep the approved APK and its known-good version.</li><li>Record the app package and signer before an update.</li><li>Stop after an error and save the report before retrying.</li></ol></div></section>`;
}
function landing() { return `
<section class="hero" aria-labelledby="page-title"><div><p class="eyebrow">Android maintenance / read-only checks</p><h1 id="page-title">Check Android update safety</h1><p class="lead">For people who maintain approved sideloaded apps when device rules or recovery paths change.</p><div class="action-row"><a class="button" href="/demo" data-route>Try it with sample data</a><p class="action-note">See a redacted report and the next safe step.</p></div><ul class="facts"><li>Read-only adb checks</li><li>Device IDs are redacted</li><li>Free for one device</li></ul></div><figure class="hero-art"><img src="/hero-concrete-moss.webp" width="1536" height="1024" fetchpriority="high" alt="A rugged handheld device and USB cable rest on concrete beside small patches of moss."></figure></section>
<section class="section" aria-labelledby="preview-title"><h2 class="section-heading" id="preview-title">See the report before you touch a device</h2>${reportMarkup()}</section>
<section class="section" id="install" aria-labelledby="install-title"><h2 class="section-heading" id="install-title">Install one small command</h2><div class="terminal" role="img" aria-label="A sample terminal session that runs the demo and writes a report."><span class="prompt">$</span> sideload-readiness demo --output readiness.md<br>Wrote readiness.md<br><br><span class="prompt">$</span> sideload-readiness check --package com.example.approved<br><span class="warn">83% — Fix the recovery check before updating.</span></div><p class="manifesto">The command asks adb for status. It does not install apps, unlock devices, or change Android settings.</p><div class="action-row"><a class="button secondary" id="download-button" href="https://github.com/B-Divyesh/sf-sideload-readiness/releases" rel="external">Open release downloads</a><a href="/install.sh">Linux and macOS installer</a><a href="/install.ps1">PowerShell installer</a></div><p id="download-status" class="action-note" aria-live="polite">Checking the download shelf.</p></section>
<section class="section" aria-labelledby="how-title"><h2 class="section-heading" id="how-title">Make one cautious maintenance decision</h2><div class="steps"><article class="step"><span class="step-number">01</span><h3>Connect</h3><p>Accept Android’s USB debugging prompt. The tool refuses an unauthorized device.</p></article><article class="step"><span class="step-number">02</span><h3>Check</h3><p>Read storage, USB state, package signer visibility, and recovery clues.</p></article><article class="step"><span class="step-number">03</span><h3>Act safely</h3><p>Save a redacted report. Follow its next step or stop before an update.</p></article></div></section>
<section class="section" aria-labelledby="limits-title"><h2 class="section-heading" id="limits-title">What this tool does and does not do</h2><div class="limits"><div><h3>It does</h3><p>Make a device-specific report from read-only adb queries. It explains what to check next.</p></div><div><h3>It does not</h3><p>It never bypasses Android controls, unlocks bootloaders, or distributes APKs.</p></div></div></section>
<section class="section fleet" id="fleet" aria-labelledby="fleet-title"><div class="fleet-grid"><div><p class="eyebrow">Small IT teams</p><h2 class="section-heading" id="fleet-title">Review several reports together</h2><p>Fleet review is a $39 one-time license. It adds a local report queue and package-status table. Single-device checks stay free.</p><p class="price">$39 <small>one-time purchase</small></p><p><a class="button" href="https://api.sociobot.in/api/v1/products/sideload-readiness/checkout" rel="external">Buy fleet review</a></p></div><form class="license-box" id="license-form"><label for="license">Have a license? Paste it here.</label><input id="license" name="license" autocomplete="off" spellcheck="false"><button type="submit">Verify fleet license</button><p id="license-status" aria-live="polite">Verification happens with Sociobot when you choose this button.</p></form></div></section>`; }
function demo() { return `<section class="page-heading"><p class="eyebrow">Demo / sample device</p><h1 id="page-title">Find the next safe step</h1><p class="lead">This sample shows a recovery check that needs review before an approved update.</p></section><section class="section">${reportMarkup()}</section><section class="section"><h2 class="section-heading">Run this on your device</h2><p class="manifesto">Install the command, connect one authorized device, then run <code>sideload-readiness check</code>. The demo never reads a device.</p><a class="button" href="/#install" data-route>See install options</a></section>`; }
function prose(title, body) { return `<section class="page-heading"><p class="eyebrow">Sideload Readiness</p><h1 id="page-title">${title}</h1></section><article class="prose">${body}</article>`; }
function privacy() { return prose('Privacy for device reports', `<p>Sideload Readiness does not collect analytics or create an account.</p><h2>What the command reads</h2><p>The command asks adb for device readiness details. It reads status such as Android version, free storage, USB state, and package signer visibility.</p><h2>What the command stores</h2><p>The command writes a report only when you ask for an output file. Exported reports replace the hardware serial with a short redacted ID.</p><h2>Fleet licenses</h2><p>If you paste a fleet license, this site sends that license to Sociobot for verification. The license result stays in this browser for up to one day.</p><h2>Contact</h2><p>For privacy questions, contact the Param Factory through Sociobot.</p>`); }
function terms() { return prose('Terms for cautious use', `<p>Sideload Readiness gives readiness information. It does not guarantee that an update will succeed.</p><h2>Use it within device policy</h2><p>Use this tool only with devices and software you are allowed to maintain. Do not use it to bypass Android security controls or organization policy.</p><h2>Fleet review license</h2><p>The $39 fleet review license is a one-time purchase. Sociobot and Dodo are the merchant of record. A refunded license can be revoked.</p><h2>No emergency recovery promise</h2><p>Follow your device maker’s approved recovery guidance. Keep known-good software before changing an approved package.</p>`); }
function missing() { return prose('That page is not here', `<div class="empty"><p>The address does not match a Sideload Readiness page.</p><a class="button" href="/" data-route>Return to the readiness check</a></div>`); }
function setDemoBanner(active) { banner.hidden = !active; banner.className = 'demo-banner'; banner.innerHTML = active ? `<strong>Demo — sample data, nothing is saved.</strong><button type="button" id="reset-demo">Reset demo</button><a href="/">Start for real</a>` : ''; if (active) document.querySelector('#reset-demo').onclick = () => { localStorage.removeItem('demo:sideload-readiness'); localStorage.setItem('demo:sideload-readiness', JSON.stringify(DEMO_REPORT)); render('/demo'); }; }
function route(path = location.pathname) { if (path === '/' && location.search.includes('demo=1')) return 'demo'; return path === '/' ? 'landing' : path.slice(1); }
function render(path = location.pathname) { const page = route(path); const content = page === 'landing' ? landing() : page === 'demo' ? demo() : page === 'privacy' ? privacy() : page === 'terms' ? terms() : missing(); app.innerHTML = content; setDemoBanner(page === 'demo'); const title = page === 'demo' ? 'Demo — Sideload Readiness' : page === 'privacy' ? 'Privacy — Sideload Readiness' : page === 'terms' ? 'Terms — Sideload Readiness' : page === 'landing' ? 'Sideload Readiness — Check Android update safety' : 'Not found — Sideload Readiness'; document.title = title; const h1 = app.querySelector('h1'); h1?.focus(); announcement.textContent = `${title} loaded`; document.querySelectorAll('[data-route]').forEach(link => link.addEventListener('click', event => { const to = new URL(link.href).pathname; if (to.startsWith('/')) { event.preventDefault(); history.pushState({}, '', to); render(to); }})); setupLicense(); if (page === 'landing') fetchDownload(); if (page === 'demo') localStorage.setItem('demo:sideload-readiness', JSON.stringify(DEMO_REPORT)); }
function setupLicense() { const form = document.querySelector('#license-form'); if (!form) return; const saved = localStorage.getItem(`sb_license:${PRODUCT}`); if (saved) form.license.value = saved; form.addEventListener('submit', async event => { event.preventDefault(); const status = document.querySelector('#license-status'); const token = form.license.value.trim(); if (!token) { status.textContent = 'Paste a license token, then verify it.'; return; } localStorage.setItem(`sb_license:${PRODUCT}`, token); status.textContent = 'Checking your license…'; try { const response = await fetch(`https://api.sociobot.in/api/v1/products/${PRODUCT}/verify?license=${encodeURIComponent(token)}`); const data = await response.json(); localStorage.setItem(`sb_license_verdict:${PRODUCT}`, JSON.stringify({ ...data, checked_at: Date.now() })); status.textContent = data.valid ? 'Fleet review is active on this browser.' : 'That license is not active. You can buy a new fleet review license.'; } catch { status.textContent = 'The license could not be checked. Your free single-device report still works.'; } }); }
async function fetchDownload() { const button = document.querySelector('#download-button'); const status = document.querySelector('#download-status'); if (!button || !status) return; const cacheKey = 'sideload-readiness:release'; let release; try { const cached = JSON.parse(localStorage.getItem(cacheKey) || 'null'); if (cached && Date.now() - cached.time < 3600000) release = cached.data; else { const response = await fetch('https://api.github.com/repos/B-Divyesh/sf-sideload-readiness/releases/latest'); if (!response.ok) throw Error('not published'); release = await response.json(); localStorage.setItem(cacheKey, JSON.stringify({time:Date.now(), data:release})); } const ua = navigator.userAgent; const wanted = ua.includes('Windows') ? 'windows' : ua.includes('Mac') ? 'macos' : 'linux'; const asset = release.assets?.find(item => item.name.toLowerCase().includes(wanted) && /\.(zip|tar\.gz|pkg|msi|deb)$/.test(item.name)); if (!asset) throw Error('missing asset'); button.href = asset.browser_download_url; button.textContent = `Download ${asset.name}`; status.textContent = 'Checksum is published with this release.'; } catch { status.textContent = 'Downloads are being published. Open the release page for the current status.'; } }
window.addEventListener('popstate', () => render());
const returnedLicense = new URLSearchParams(location.search).get('license'); if (returnedLicense) { localStorage.setItem(`sb_license:${PRODUCT}`, returnedLicense); const clean = location.pathname; history.replaceState({}, '', clean); }
render();
